package com.vpxwatcher.app.data

/**
 * Records and session stats — Firebase uploads for records/stats have been removed.
 * Data is now stored locally on the desktop only and no longer synced to Firebase.
 * This class is kept as a stub to avoid breaking any remaining references.
 */
class RecordsRepository
